services:
  - type: web
    name: apg-full
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APG_SECRET_KEY
        generateValue: true
