const $=s=>document.querySelector(s), esc=x=>String(x??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));
let me=null, view='home', nav=[];
async function api(url,opt={}){let r=await fetch(url,opt);let d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Ошибка запроса');return d}
function toast(t){let x=document.createElement('div');x.className='toast';x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2600)}
function page(html,title='APG'){document.title=title+' — APG';$('#app').innerHTML=`<aside class="sidebar"><div class="brand">APG<small>АВТО ПАРТНЁРСКАЯ ГРУППА</small></div><div class="nav">${nav.map(n=>`<button class="${view===n.id?'active':''}" onclick="go('${n.id}')">${n.icon} ${n.name}</button>`).join('')}</div><div class="sidebar-footer"><button class="btn" style="width:100%" onclick="logout()">Выйти</button></div></aside><main class="main"><div class="top"><div><div class="title">${title}</div><div class="sub">${esc(me.name)} · ${esc(me.role)}</div></div><button class="btn" onclick="profile()">${me.avatar?`<img class="avatar" src="${me.avatar}" style="width:38px;height:38px">`:'Профиль'}</button></div>${html}</main><nav class="bottom">${nav.slice(0,5).map(n=>`<button class="${view===n.id?'active':''}" onclick="go('${n.id}')">${n.icon}<br>${n.name}</button>`).join('')}</nav>`}
function navFor(){if(me.role==='owner')return [{id:'home',name:'Обзор',icon:'⌂'},{id:'companies',name:'Компании',icon:'▦'},{id:'offers',name:'Модерация',icon:'◆'},{id:'bookings',name:'Записи',icon:'◷'},{id:'content',name:'Контент',icon:'✦'},{id:'users',name:'Пользователи',icon:'◎'},{id:'reviews',name:'Отзывы',icon:'★'},{id:'promos',name:'Промокоды',icon:'%'},{id:'plans',name:'Тарифы',icon:'₽'},{id:'audit',name:'Аудит',icon:'≡'},{id:'stats',name:'Статистика',icon:'↗'}];if(me.role==='seller')return [{id:'home',name:'Обзор',icon:'⌂'},{id:'myoffers',name:'Мои скидки',icon:'◆'},{id:'bookings',name:'Записи',icon:'◷'},{id:'qr',name:'QR',icon:'▣'},{id:'plans',name:'Тариф',icon:'₽'},{id:'stats',name:'Статистика',icon:'↗'},{id:'profile',name:'Профиль',icon:'◎'}];return [{id:'home',name:'Главная',icon:'⌂'},{id:'notifications',name:'Уведомления',icon:'●'},{id:'offers',name:'Скидки',icon:'◆'},{id:'companies',name:'Компании',icon:'▦'},{id:'bookings',name:'Записи',icon:'◷'},{id:'favorites',name:'Избранное',icon:'♥'},{id:'promos',name:'Промокоды',icon:'%'},{id:'cars',name:'Гараж',icon:'🚗'},{id:'history',name:'История',icon:'◷'},{id:'profile',name:'Профиль',icon:'◎'}]}
async function start(){try{let d=await api('/api/me');window.apgCsrf=d.csrf||'';if(!d.user)return auth();me=d.user;nav=navFor();go('home')}catch(e){auth()}}
function auth(){view='login';$('#app').innerHTML=`<main class="auth"><div class="authbox"><div class="brand">APG<small>АВТО ПАРТНЁРСКАЯ ГРУППА</small></div><h1>Вход в APG</h1><p class="muted">Партнёрская платформа для автомобильного бизнеса.</p><form onsubmit="event.preventDefault();login(this)"><div class="field"><label>EMAIL</label><input name="email" type="email" required placeholder="email@example.com"></div><div class="field"><label>ПАРОЛЬ</label><input name="password" type="password" required></div><button class="btn primary" style="width:100%">Войти</button></form><button class="btn" style="width:100%;margin-top:9px" onclick="registerForm()">Создать аккаунт</button><div class="notice" style="margin-top:15px">Demo: owner@apg.local / apg1234 · seller@apg.local / seller1234 · user@apg.local / user1234</div></div></main>`}
function registerForm(){let x=$('#app');x.innerHTML=`<main class="auth"><div class="authbox"><div class="brand">APG</div><h1>Регистрация</h1><form onsubmit="event.preventDefault();register(this)"><div class="field"><label>ИМЯ</label><input name="name" required></div><div class="field"><label>EMAIL</label><input name="email" type="email" required></div><div class="field"><label>ПАРОЛЬ</label><input name="password" type="password" minlength="6" required></div><button class="btn primary" style="width:100%">Зарегистрироваться</button></form><button class="btn" style="width:100%;margin-top:9px" onclick="auth()">Назад</button></div></main>`}
async function login(f){try{let data=new FormData(f);data.append('csrf',window.apgCsrf||'');let d=await api('/api/login',{method:'POST',body:data});me=d.user;nav=navFor();go('home')}catch(e){toast(e.message)}}
async function register(f){try{let data=new FormData(f);data.append('csrf',window.apgCsrf||'');let d=await api('/api/register',{method:'POST',body:data});me=d.user;nav=navFor();go('home')}catch(e){toast(e.message)}}
async function logout(){try{let data=new FormData();data.append('csrf',window.apgCsrf||'');await api('/api/logout',{method:'POST',body:data})}finally{me=null;window.apgCsrf='';auth()}}
function go(v){view=v;({home:dashboard,companies:companies,offers:me?.role==='owner'?moderation:offers,favorites:favorites,cars:cars,history:history,myoffers:myoffers,qr:qrPage,content:content,users:users,reviews:reviews,audit:audit,stats:stats,profile:profile,notifications:notifications,bookings:bookingsPage,promos:promosPage,plans:plansPage}[v]||dashboard)()}
function dashboard(){if(me.role==='customer')customerHome();else if(me.role==='seller')sellerHome();else ownerHome()}
async function ownerHome(){let s=await api('/api/stats'),pending=s.pending_offers||0;page(`<section class="hero"><h1>Контроль APG<br>в одном месте.</h1><p>Компании, продавцы, скидки, контент, отзывы и QR-механика. Все действия Owner фиксируются в аудите.</p><div class="row"><button class="btn primary" onclick="go('companies')">Управление компаниями</button><button class="btn" onclick="go('offers')">${pending} на модерации</button></div></section><div class="grid4">${kpis(s)}</div><div class="grid2" style="margin-top:16px"><div class="card"><h2>Что нового</h2><div class="list"><div class="listitem">◆ Новые скидки проходят модерацию</div><div class="listitem">✦ Галерея и услуги управляются Owner</div><div class="listitem">★ Отзывы публикуются после проверки</div><div class="listitem">▣ QR одноразовый и живёт 5 минут</div></div></div><div class="card"><h2>Быстрые действия</h2><div class="row"><button class="btn" onclick="go('content')">Добавить контент</button><button class="btn" onclick="go('users')">Назначить Seller</button><button class="btn" onclick="go('reviews')">Отзывы</button></div></div></div>`,'Обзор')}
async function sellerHome(){let s=await api('/api/stats'),cs=await api('/api/seller/companies');page(`<section class="hero"><h1>Кабинет<br>партнёра.</h1><p>Работай только с назначенными компаниями. Созданная скидка уходит Owner на модерацию.</p><div class="row">${cs.map(c=>`<span class="badge">${esc(c.name)}</span>`).join('')}</div></section><div class="grid4">${kpis(s)}</div><div class="card" style="margin-top:16px"><h2>Действия</h2><button class="btn primary" onclick="offerModal()">Создать скидку</button> <button class="btn" onclick="go('myoffers')">Мои скидки</button> <button class="btn" onclick="go('qr')">Проверить QR</button></div>`,'Обзор')}
async function customerHome(){let [cs,os]=await Promise.all([api('/api/companies'),api('/api/offers')]);page(`<section class="hero"><h1>Авто-партнёры<br>APG.</h1><p>Находи предложения, сохраняй компании, смотри услуги и галерею. Получай одноразовый QR для подтверждения скидки.</p><div class="row"><button class="btn primary" onclick="go('offers')">Смотреть скидки</button><button class="btn" onclick="go('companies')">Компании</button></div></section><div class="grid2"><div class="card"><div class="split"><h2>Активные скидки</h2><span class="badge">${os.length}</span></div><div class="list">${os.slice(0,4).map(o=>`<div class="listitem"><div><b>${esc(o.title)}</b><div class="muted">${esc(o.company_name)}</div></div><button class="btn small" onclick="makeQR(${o.id})">QR</button></div>`).join('')||'<div class="empty">Активных скидок пока нет</div>'}</div></div><div class="card"><div class="split"><h2>Партнёры</h2><span class="badge">${cs.length}</span></div><div class="list">${cs.slice(0,5).map(c=>`<div class="listitem"><div class="avatar">${c.logo?`<img src="${c.logo}">`:esc(c.name[0])}</div><div style="flex:1"><b>${esc(c.name)}</b><div class="muted">${esc(c.category)} · ${c.services} услуг</div></div><button class="btn small" onclick="companyPage(${c.id})">Открыть</button></div>`).join('')}</div></div></div>`,'Главная')}
function kpis(s){let a=[['companies','Компании'],['offers','Активные скидки'],['pending_offers','На модерации'],['qr_used','Погашено QR']];return a.map(([k,l])=>`<div class="card stat"><b>${s[k]??0}</b><span>${l}</span></div>`).join('')}
async function companies(){let cs=await api('/api/companies');page(`<div class="search"><input id="cq" placeholder="Поиск компаний, адреса…" onkeydown="if(event.key==='Enter')companySearch()"><button class="btn primary" onclick="companySearch()">Поиск</button></div><div class="grid">${cs.map(c=>companyCard(c)).join('')||'<div class="empty">Компаний нет</div>'}</div>`,'Компании')}
async function companySearch(){let q=$('#cq').value;let cs=await api('/api/companies?q='+encodeURIComponent(q));$('.main').lastElementChild.innerHTML=cs.map(c=>companyCard(c)).join('')||'<div class="empty">Ничего не найдено</div>'}
function companyCard(c){return `<div class="card company"><div class="biglogo" style="min-height:180px">${c.cover?`<img src="${c.cover}">`:c.logo?`<img src="${c.logo}">`:`${esc(c.name[0]||'A')}`}</div><div class="split"><div><h3>${esc(c.name)}</h3><div class="muted">${esc(c.category)} · ${c.offers} скидок · ${c.services} услуг</div></div>${me.role==='customer'?`<button class="btn small" onclick="fav(${c.id})">${c.favorite?'♥':'♡'}</button>`:''}</div><p class="muted">${esc(c.description)}</p><button class="btn" onclick="companyPage(${c.id})">Открыть профиль</button></div>`}
async function companyPage(id,tab='offers'){let c=await api('/api/companies/'+id);let tabs=['offers','gallery','services','reviews'];page(`<div class="detail-head"><div class="biglogo">${c.cover?`<img src="${c.cover}">`:c.logo?`<img src="${c.logo}">`:esc(c.name[0])}</div><div class="card"><span class="badge">${esc(c.category)}</span><h1>${esc(c.name)}</h1><p class="muted">${esc(c.description)}</p><div class="list"><div class="listitem">⌖ ${esc(c.address)}</div><div class="listitem">☎ ${esc(c.phone)}</div><div class="listitem">◷ ${esc(c.hours)}</div></div>${me.role==='customer'?`<button class="btn primary" style="margin-top:12px" onclick="fav(${id})">${'Добавить в избранное'}</button>`:''}</div></div><div class="tabs" style="margin-top:20px">${tabs.map(t=>`<button class="btn ${tab===t?'primary':''}" onclick="companyPage(${id},'${t}')">${({offers:'Скидки',gallery:'Галерея',services:'Услуги',reviews:'Отзывы'})[t]}</button>`).join('')}</div>${tab==='offers'?companyOffers(c):tab==='gallery'?companyGallery(c):tab==='services'?companyServices(c):companyReviews(c)}`,'Компания')}
function companyOffers(c){return `<div class="grid">${c.offers.map(o=>`<div class="card offer">${o.image?`<img src="${o.image}">`:''}<span class="badge good">АКТИВНА</span><h3>${esc(o.title)}</h3><p class="muted">${esc(o.description)}</p><b>${esc(o.discount)}</b><div class="row" style="margin-top:14px"><button class="btn primary" onclick="makeQR(${o.id})">Получить QR</button><span class="muted">до ${esc(o.valid_until||'без срока')}</span></div></div>`).join('')||'<div class="empty">У компании пока нет активных скидок</div>'}</div>`}
function companyGallery(c){return `<div class="gallery">${c.gallery.map(g=>`<div><img src="${g.image}" alt=""><div style="padding:8px 2px"><b>${esc(g.title)}</b><div class="muted">${esc(g.caption)}</div></div></div>`).join('')||'<div class="empty">Галерея пока пустая</div>'}</div>`}
function companyServices(c){return `<div class="grid2">${c.services.map(s=>`<div class="card service">${s.image?`<img src="${s.image}">`:''}<div><h3>${esc(s.title)}</h3><p class="muted">${esc(s.description)}</p><b>${esc(s.price||'Цена по запросу')}</b></div></div>`).join('')||'<div class="empty">Услуги пока не добавлены</div>'}</div>`}
function companyReviews(c){return `<div class="grid2">${c.reviews.map(r=>`<div class="card"><div class="split"><b>${esc(r.user_name)}</b><span class="review-stars">${'★'.repeat(r.rating)}${'☆'.repeat(5-r.rating)}</span></div><p>${esc(r.body)}</p></div>`).join('')||'<div class="empty">Одобренных отзывов пока нет</div>'}</div>${me.role==='customer'?`<button class="btn primary" style="margin-top:15px" onclick="reviewModal(${c.id})">Оставить отзыв</button>`:''}`}
async function offers(){let os=await api('/api/offers');page(`<div class="search"><input id="oq" placeholder="Поиск скидок…"><button class="btn primary" onclick="offerSearch()">Найти</button></div><div class="grid">${os.map(o=>`<div class="card offer">${o.image?`<img src="${o.image}">`:''}<span class="badge good">ACTIVE</span><h3>${esc(o.title)}</h3><div class="muted">${esc(o.company_name)}</div><p>${esc(o.description)}</p><b>${esc(o.discount)}</b><div class="row" style="margin-top:14px"><button class="btn primary" onclick="makeQR(${o.id})">QR скидки</button><button class="btn" onclick="companyPage(${o.company_id})">Компания</button></div></div>`).join('')||'<div class="empty">Скидок пока нет</div>'}</div>`,'Скидки')}
async function offerSearch(){let q=$('#oq').value;let os=await api('/api/offers?q='+encodeURIComponent(q));$('.main').lastElementChild.innerHTML=os.map(o=>`<div class="card offer">${o.image?`<img src="${o.image}">`:''}<h3>${esc(o.title)}</h3><div class="muted">${esc(o.company_name)}</div><p>${esc(o.description)}</p><b>${esc(o.discount)}</b></div>`).join('')||'<div class="empty">Ничего не найдено</div>'}
async function fav(id){try{await api('/api/favorites/'+id,{method:'POST'});toast('Избранное обновлено');if(view==='companies')companies()}catch(e){toast(e.message)}}
async function favorites(){let cs=await api('/api/favorites');page(`<div class="grid">${cs.map(c=>companyCard(c)).join('')||'<div class="empty">Избранных компаний пока нет</div>'}</div>`,'Избранное')}
async function cars(){let a=await api('/api/cars');page(`<div class="split"><div><h2>Мой гараж</h2><p class="muted">Добавляй автомобили без поля марки.</p></div><button class="btn primary" onclick="carModal()">Добавить авто</button></div><div class="grid" style="margin-top:16px">${a.map(c=>`<div class="card">${c.photo?`<img class="cover" style="height:150px" src="${c.photo}">`:''}<h2>${esc(c.model||'Автомобиль')}</h2><div class="muted">${esc(c.year)} · ${esc(c.plate)}</div><p class="muted">VIN: ${esc(c.vin||'—')}<br>Пробег: ${esc(c.mileage||'—')}</p><button class="btn danger" onclick="delCar(${c.id})">Удалить</button></div>`).join('')||'<div class="empty">Гараж пуст</div>'}</div>`,'Гараж')}
function carModal(){modal('Добавить автомобиль',`<form onsubmit="event.preventDefault();addCar(this)"><div class="field"><label>МОДЕЛЬ</label><input name="model" placeholder="Например, A6"></div><div class="formgrid"><div class="field"><label>ГОД</label><input name="year"></div><div class="field"><label>ГОСНОМЕР</label><input name="plate"></div></div><div class="field"><label>VIN</label><input name="vin"></div><div class="field"><label>ПРОБЕГ</label><input name="mileage"></div><div class="field"><label>ФОТО</label><input name="photo" type="file" accept="image/*"></div><button class="btn primary">Сохранить</button></form>`)}
async function addCar(f){await api('/api/cars',{method:'POST',body:new FormData(f)});document.querySelector('.modal')?.remove();toast('Автомобиль добавлен');cars()}
async function delCar(id){await api('/api/cars/'+id,{method:'DELETE'});cars()}
async function history(){let a=await api('/api/history');page(`<div class="split"><div><h2>История обслуживания</h2><p class="muted">Здесь сохраняются записи о работах и обслуживании.</p></div><button class="btn primary" onclick="historyModal()">Добавить запись</button></div><div class="grid2" style="margin-top:16px">${a.map(x=>`<div class="card"><span class="badge">${esc(x.service_date)}</span><h3>${esc(x.title)}</h3><div class="muted">${esc(x.company_name||'APG')} · ${esc(x.car_model||'Авто')}</div><p class="muted">${esc(x.note)}</p></div>`).join('')||'<div class="empty">История пока пустая</div>'}</div>`,'История')}
function historyModal(){modal('Новая запись',`<form onsubmit="event.preventDefault();addHistory(this)"><div class="field"><label>РАБОТА</label><input name="title" required placeholder="ТО, диагностика…"></div><div class="field"><label>ДАТА</label><input name="service_date" type="date"></div><div class="field"><label>ЗАМЕТКА</label><textarea name="note"></textarea></div><button class="btn primary">Добавить</button></form>`)}
async function addHistory(f){await api('/api/history',{method:'POST',body:new FormData(f)});document.querySelector('.modal')?.remove();history()}
async function profile(){let d=await api('/api/profile');let u=d.user,s=d.settings;page(`<div class="grid2"><div class="card"><div class="split"><div><div class="avatar" style="width:86px;height:86px">${u.avatar?`<img src="${u.avatar}">`:esc(u.name[0])}</div><h2>${esc(u.name)}</h2><div class="muted">${esc(u.email)}</div><span class="badge">${esc(u.role)}</span></div></div><div class="divider"></div><form onsubmit="event.preventDefault();saveProfile(this)"><div class="field"><label>ИМЯ</label><input name="name" value="${esc(u.name)}"></div><div class="field"><label>ТЕЛЕФОН</label><input name="phone" value="${esc(u.phone)}"></div><div class="field"><label>USERNAME</label><input name="username" value="${esc(u.username)}"></div><div class="field"><label>АВАТАР</label><input name="avatar" type="file" accept="image/*"></div><button class="btn primary">Сохранить профиль</button></form></div><div class="card"><h2>Визуальный кастом</h2><p class="muted">Базовая система настроек уже готова для расширения тем.</p><form onsubmit="event.preventDefault();saveSettings(this)"><div class="field"><label>АКЦЕНТ</label><select name="accent"><option value="white">White</option><option value="silver">Silver</option><option value="lime">Lime</option><option value="blue">Blue</option></select></div><div class="field"><label>ПЛОТНОСТЬ</label><select name="density"><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></div><label><input type="checkbox" name="animations" checked> Анимации</label><br><br><button class="btn primary">Применить</button></form></div></div>`,'Профиль')}
async function saveProfile(f){let d=await api('/api/profile',{method:'POST',body:new FormData(f)});me=d.user;toast('Профиль сохранён');profile()}
async function saveSettings(f){await api('/api/settings',{method:'POST',body:new FormData(f)});toast('Настройки сохранены')}
async function stats(){let s=await api('/api/stats');page(`<div class="grid4">${kpis(s)}</div><div class="grid2" style="margin-top:16px"><div class="card"><h2>QR</h2><div class="notice">Выдано: <b>${s.qr_issued}</b> · Погашено: <b>${s.qr_used}</b></div></div><div class="card"><h2>Пользователи</h2><div class="notice">Клиенты: ${s.customers} · Sellers: ${s.sellers}</div></div></div>`,'Статистика')}
async function moderation(){let a=await api('/api/owner/offers');page(`<div class="grid">${a.map(o=>`<div class="card offer"><span class="badge ${o.status==='active'?'good':o.status==='pending'?'warn':'bad'}">${esc(o.status)}</span><h3>${esc(o.title)}</h3><div class="muted">${esc(o.company_name)} · ${esc(o.creator_name||'Owner')}</div><p>${esc(o.description)}</p><b>${esc(o.discount)}</b><div class="row" style="margin-top:14px">${o.status!=='active'?`<button class="btn primary" onclick="mod(${o.id},'active')">Одобрить</button>`:''}${o.status!=='rejected'?`<button class="btn danger" onclick="mod(${o.id},'rejected')">Отклонить</button>`:''}<button class="btn" onclick="mod(${o.id},'disabled')">Отключить</button><button class="btn danger" onclick="delOffer(${o.id})">Удалить</button></div></div>`).join('')||'<div class="empty">Скидок нет</div>'}</div>`,'Модерация')}
async function mod(id,status){let f=new FormData();f.append('status',status);await api('/api/owner/offers/'+id,{method:'PATCH',body:f});toast('Статус изменён');moderation()}
async function delOffer(id){await api('/api/owner/offers/'+id,{method:'DELETE'});moderation()}
async function myoffers(){let a=await api('/api/seller/offers');page(`<div class="split"><div><h2>Мои скидки</h2><p class="muted">Только предложения, созданные твоим Seller-аккаунтом.</p></div><button class="btn primary" onclick="offerModal()">Создать</button></div><div class="grid" style="margin-top:16px">${a.map(o=>`<div class="card"><span class="badge ${o.status==='active'?'good':o.status==='pending'?'warn':'bad'}">${o.status}</span><h3>${esc(o.title)}</h3><div class="muted">${esc(o.company_name)}</div><p>${esc(o.description)}</p><div class="row"><button class="btn danger" onclick="sellerDel(${o.id})">Удалить</button></div></div>`).join('')||'<div class="empty">У тебя пока нет скидок</div>'}</div>`,'Мои скидки')}
async function sellerDel(id){await api('/api/seller/offers/'+id,{method:'DELETE'});myoffers()}
async function offerModal(){let cs=await api('/api/seller/companies');modal('Новая скидка',`<form onsubmit="event.preventDefault();sellerAdd(this)"><div class="field"><label>КОМПАНИЯ</label><select name="company_id" required>${cs.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>НАЗВАНИЕ</label><input name="title" required></div><div class="field"><label>ОПИСАНИЕ</label><textarea name="description"></textarea></div><div class="formgrid"><div class="field"><label>СКИДКА</label><input name="discount" placeholder="-20%"></div><div class="field"><label>ДЕЙСТВУЕТ ДО</label><input name="valid_until" type="date"></div></div><div class="field"><label>ВИЗУАЛ</label><input name="image" type="file" accept="image/*"></div><div class="notice">После сохранения предложение получает статус <b>pending</b> и становится видимым клиентам только после Owner-модерации.</div><br><button class="btn primary">Отправить</button></form>`)}
async function sellerAdd(f){await api('/api/seller/offers',{method:'POST',body:new FormData(f)});document.querySelector('.modal')?.remove();toast('Отправлено на модерацию');myoffers()}
async function qrPage(){page(`<div class="grid2"><div class="card"><h2>Проверка QR</h2><p class="muted">Можно вставить APG-токен. Камера телефона подключается следующим этапом через BarcodeDetector.</p><form onsubmit="event.preventDefault();redeem()"><div class="field"><label>QR TOKEN</label><input id="qt" required placeholder="APG:…"></div><button class="btn primary">Подтвердить скидку</button></form></div><div class="card"><h2>Правила</h2><div class="list"><div class="listitem">5 минут жизни QR</div><div class="listitem">Одно использование</div><div class="listitem">Seller видит только свои компании</div><div class="listitem">Owner может подтвердить любой QR</div></div></div></div>`,'QR')}
async function redeem(){let f=new FormData();f.append('token',$('#qt').value);try{let d=await api('/api/redeem',{method:'POST',body:f});toast(d.message);$('#qt').value=''}catch(e){toast(e.message)}}
async function makeQR(id){try{let d=await api('/api/offers/'+id+'/qr',{method:'POST'});modal('QR скидки',`<div class="center"><p class="muted">${esc(d.title)}</p><div class="qr"><img src="${d.image}"></div><h3>Действует 5 минут</h3><div class="notice" style="word-break:break-all">${esc(d.token)}</div></div>`)}catch(e){toast(e.message)}}
async function content(){let cs=await api('/api/owner/companies');page(`<div class="split"><div><h2>Контент партнёров</h2><p class="muted">Owner управляет компаниями, галереями и услугами.</p></div><button class="btn primary" onclick="companyModal()">Новая компания</button></div><div class="grid2" style="margin-top:16px">${cs.map(c=>`<div class="card"><div class="split"><div><h2>${esc(c.name)}</h2><div class="muted">${esc(c.category)} · Sellers: ${c.seller_count}</div></div><span class="badge">${c.active?'ACTIVE':'OFF'}</span></div><div class="row" style="margin-top:15px"><button class="btn" onclick="contentModal(${c.id},'gallery')">Галерея</button><button class="btn" onclick="contentModal(${c.id},'services')">Услуги</button><button class="btn" onclick="editCompany(${c.id})">Изменить</button><button class="btn danger" onclick="deleteCompany(${c.id})">Удалить</button></div></div>`).join('')}</div>`,'Контент')}
async function companyModal(){modal('Новая компания',`<form onsubmit="event.preventDefault();createCompany(this)"><div class="field"><label>НАЗВАНИЕ</label><input name="name" required></div><div class="field"><label>ОПИСАНИЕ</label><textarea name="description"></textarea></div><div class="formgrid"><div class="field"><label>КАТЕГОРИЯ</label><input name="category" value="Автосервис"></div><div class="field"><label>ТЕЛЕФОН</label><input name="phone"></div></div><div class="field"><label>АДРЕС</label><input name="address"></div><div class="field"><label>ГРАФИК</label><input name="hours"></div><div class="formgrid"><div class="field"><label>ЛОГОТИП</label><input name="logo" type="file" accept="image/*"></div><div class="field"><label>ОБЛОЖКА</label><input name="cover" type="file" accept="image/*"></div></div><button class="btn primary">Создать</button></form>`)}
async function createCompany(f){await api('/api/owner/companies',{method:'POST',body:new FormData(f)});document.querySelector('.modal')?.remove();content()}
async function editCompany(id){let cs=await api('/api/owner/companies'),c=cs.find(x=>x.id===id);modal('Редактировать компанию',`<form onsubmit="event.preventDefault();updateCompany(${id},this)"><div class="field"><label>НАЗВАНИЕ</label><input name="name" value="${esc(c.name)}" required></div><div class="field"><label>ОПИСАНИЕ</label><textarea name="description">${esc(c.description)}</textarea></div><div class="formgrid"><div class="field"><label>КАТЕГОРИЯ</label><input name="category" value="${esc(c.category)}"></div><div class="field"><label>ТЕЛЕФОН</label><input name="phone" value="${esc(c.phone)}"></div></div><div class="field"><label>АДРЕС</label><input name="address" value="${esc(c.address)}"></div><div class="field"><label>ГРАФИК</label><input name="hours" value="${esc(c.hours)}"></div><div class="field"><label>СТАТУС</label><select name="active"><option value="1" ${c.active?'selected':''}>Активна</option><option value="0" ${!c.active?'selected':''}>Выключена</option></select></div><button class="btn primary">Сохранить</button></form>`)}
async function updateCompany(id,f){await api('/api/owner/companies/'+id,{method:'PATCH',body:new FormData(f)});document.querySelector('.modal')?.remove();content()}
async function deleteCompany(id){if(!confirm('Удалить компанию?'))return;await api('/api/owner/companies/'+id,{method:'DELETE'});content()}
function contentModal(cid,type){let gallery=type==='gallery';modal(gallery?'Галерея':'Услуга',`<form onsubmit="event.preventDefault();submitContent(${cid},'${type}',this)"><div class="field"><label>${gallery?'ЗАГОЛОВОК':'НАЗВАНИЕ'}</label><input name="title" required></div><div class="field"><label>ОПИСАНИЕ</label><textarea name="description"></textarea></div>${gallery?'<div class="field"><label>ПОДПИСЬ</label><input name="caption"></div>':'<div class="field"><label>ЦЕНА</label><input name="price" placeholder="от 50 €"></div>'}<div class="field"><label>ФОТО</label><input name="image" type="file" accept="image/*" required></div><button class="btn primary">Добавить</button></form>`)}
async function submitContent(cid,type,f){let d=new FormData(f);d.append('company_id',cid);await api(type==='gallery'?'/api/owner/gallery':'/api/owner/services',{method:'POST',body:d});document.querySelector('.modal')?.remove();toast('Контент добавлен');content()}
async function reviews(){let a=await api('/api/owner/reviews');page(`<div class="grid">${a.map(r=>`<div class="card"><span class="badge ${r.status==='approved'?'good':r.status==='pending'?'warn':'bad'}">${r.status}</span><h3>${esc(r.company_name)}</h3><div class="review-stars">${'★'.repeat(r.rating)}${'☆'.repeat(5-r.rating)}</div><p>${esc(r.body)}</p><div class="muted">${esc(r.user_name)}</div><div class="row" style="margin-top:12px"><button class="btn primary" onclick="reviewMod(${r.id},'approved')">Опубликовать</button><button class="btn danger" onclick="reviewMod(${r.id},'rejected')">Скрыть</button><button class="btn danger" onclick="reviewDel(${r.id})">Удалить</button></div></div>`).join('')||'<div class="empty">Отзывы отсутствуют</div>'}</div>`,'Отзывы')}
async function reviewMod(id,s){let f=new FormData();f.append('status',s);await api('/api/owner/reviews/'+id,{method:'PATCH',body:f});reviews()}
async function reviewDel(id){await api('/api/owner/reviews/'+id,{method:'DELETE'});reviews()}
function reviewModal(cid){modal('Отзыв',`<form onsubmit="event.preventDefault();addReview(${cid},this)"><div class="field"><label>ОЦЕНКА</label><select name="rating"><option value="5">5 — отлично</option><option value="4">4</option><option value="3">3</option><option value="2">2</option><option value="1">1</option></select></div><div class="field"><label>ТЕКСТ</label><textarea name="body" required></textarea></div><button class="btn primary">Отправить</button></form>`)}
async function addReview(cid,f){let d=new FormData(f);d.append('company_id',cid);await api('/api/reviews',{method:'POST',body:d});document.querySelector('.modal')?.remove();toast('Отзыв отправлен на модерацию')}
async function users(){let a=await api('/api/owner/users'),cs=await api('/api/owner/companies');page(`<div class="grid2"><div class="card"><h2>Пользователи</h2><div style="overflow:auto"><table class="table"><thead><tr><th>USER</th><th>ROLE</th><th>STATUS</th><th></th></tr></thead><tbody>${a.map(u=>`<tr><td><b>${esc(u.name)}</b><br><span class="muted">${esc(u.email)}</span></td><td>${esc(u.role)}</td><td>${u.active?'ACTIVE':'OFF'}</td><td>${u.role!=='owner'?`<button class="btn small" onclick="toggleUser(${u.id},${u.active?0:1})">${u.active?'Отключить':'Включить'}</button>`:''}</td></tr>`).join('')}</tbody></table></div></div><div class="card"><h2>Назначить Seller</h2><form onsubmit="event.preventDefault();assignSeller(this)"><div class="field"><label>SELLER</label><select name="seller_id">${a.filter(u=>u.role==='seller').map(u=>`<option value="${u.id}">${esc(u.name)} · ${esc(u.email)}</option>`).join('')}</select></div><div class="field"><label>КОМПАНИЯ</label><select name="company_id">${cs.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><button class="btn primary">Назначить</button></form></div></div>`,'Пользователи')}
async function toggleUser(id,active){let f=new FormData();f.append('active',active);await api('/api/owner/users/'+id,{method:'PATCH',body:f});users()}
async function assignSeller(f){await api('/api/owner/assign-seller',{method:'POST',body:new FormData(f)});toast('Seller назначен');users()}
async function audit(){let a=await api('/api/owner/audit');page(`<div class="card"><h2>Журнал действий</h2><div style="overflow:auto"><table class="table"><thead><tr><th>ВРЕМЯ</th><th>ПОЛЬЗОВАТЕЛЬ</th><th>ДЕЙСТВИЕ</th><th>ДЕТАЛИ</th></tr></thead><tbody>${a.map(x=>`<tr><td>${esc(x.created_at)}</td><td>${esc(x.user_name||'system')}</td><td>${esc(x.title)}</td><td>${esc(x.details)}</td></tr>`).join('')}</tbody></table></div></div>`,'Аудит')}
function modal(title,body){let x=document.createElement('div');x.className='modal';x.innerHTML=`<div class="modalbox"><button class="close" onclick="this.closest('.modal').remove()">×</button><h2>${title}</h2>${body}</div>`;document.body.appendChild(x)}
if('serviceWorker' in navigator) navigator.serviceWorker.register('/static/sw.js').catch(()=>{});
start();


/* APG V8 enhancements */
let apgInstallPrompt=null, apgScannerStream=null, apgScanTimer=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();apgInstallPrompt=e;window.dispatchEvent(new Event('apg-install-ready'))});
async function installAPG(){if(!apgInstallPrompt){toast('Установка будет доступна из меню браузера');return}apgInstallPrompt.prompt();await apgInstallPrompt.userChoice;apgInstallPrompt=null}
function mapsLink(address){return address?`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}`:''}
function shareText(text,url=location.href){if(navigator.share)return navigator.share({title:'APG',text,url}).catch(()=>{});navigator.clipboard?.writeText(url).then(()=>toast('Ссылка скопирована')).catch(()=>toast(text))}
async function notifications(){let a=await api('/api/notifications');page(`<div class="split"><div><h2>Уведомления</h2><p class="muted">Системные события APG и статусы заявок.</p></div><button class="btn" onclick="readNotifications()">Отметить прочитанными</button></div><div class="grid2" style="margin-top:16px">${a.map(n=>`<div class="card ${n.read?'':'unread'}"><div class="split"><b>${esc(n.title)}</b><span class="badge">${esc((n.created_at||'').slice(0,16).replace('T',' '))}</span></div><p class="muted">${esc(n.body)}</p></div>`).join('')||'<div class="empty">Уведомлений нет</div>'}</div>`,'Уведомления')}
async function readNotifications(){await api('/api/notifications/read',{method:'POST'});notifications()}
async function qrHistory(){let a=await api('/api/qr-history');page(`<div class="card"><div class="split"><div><h2>История QR</h2><p class="muted">Выданные и погашенные QR-коды.</p></div><button class="btn" onclick="qrPage()">Назад</button></div><div style="overflow:auto;margin-top:15px"><table class="table"><thead><tr><th>ПРЕДЛОЖЕНИЕ</th><th>КОМПАНИЯ</th><th>КЛИЕНТ</th><th>СТАТУС</th><th>ПОГАШЕН</th></tr></thead><tbody>${a.map(x=>`<tr><td>${esc(x.offer_title)}</td><td>${esc(x.company_name)}</td><td>${esc(x.customer_name)}</td><td>${x.used?'USED':'ACTIVE / EXPIRED'}</td><td>${esc(x.used_at||'—')}</td></tr>`).join('')||'<tr><td colspan="5">Нет данных</td></tr>'}</tbody></table></div></div>`,'История QR')}
async function qrPage(){page(`<div class="grid2"><div class="card"><div class="split"><div><h2>QR-проверка</h2><p class="muted">Наведи камеру на QR клиента — токен распознается автоматически.</p></div><span class="badge good">LIVE</span></div><div id="qrScanner" class="scanner"><video id="qrVideo" playsinline muted></video><div class="scanline"></div><div class="scanner-hint">Разреши камеру и держи QR в рамке</div></div><div class="row" style="margin-top:12px"><button class="btn primary" onclick="startQRScanner()">Запустить камеру</button><button class="btn" onclick="stopQRScanner()">Остановить</button></div><div class="divider"></div><form onsubmit="event.preventDefault();redeem()"><div class="field"><label>ИЛИ ВВЕДИ ТОКЕН</label><input id="qt" required placeholder="APG:…"></div><button class="btn primary">Подтвердить скидку</button></form></div><div class="card"><h2>Контроль</h2><div class="list"><div class="listitem">⏱ QR действует 5 минут</div><div class="listitem">✓ QR можно использовать один раз</div><div class="listitem">🔐 Seller проверяет только назначенные компании</div><div class="listitem">▣ Проверка работает через камеру телефона</div></div><button class="btn" style="margin-top:15px" onclick="qrHistory()">История QR</button></div></div>`,'QR')}
async function startQRScanner(){
  stopQRScanner();
  const video=document.getElementById('qrVideo');
  if(!video)return;
  if(!('BarcodeDetector' in window)){toast('Браузер не поддерживает сканирование QR. Используй ввод токена.');return}
  try{
    apgScannerStream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:720}},audio:false});
    video.srcObject=apgScannerStream; await video.play();
    const detector=new BarcodeDetector({formats:['qr_code']});
    const loop=async()=>{if(!apgScannerStream)return;try{const codes=await detector.detect(video);if(codes.length){let v=codes[0].rawValue||'';if(v.startsWith('APG:')){document.getElementById('qt').value=v;toast('QR распознан');await redeem();stopQRScanner();return}}}catch(e){}apgScanTimer=requestAnimationFrame(loop)};
    loop();
  }catch(e){toast('Не удалось открыть камеру: '+(e.message||'проверь HTTPS и разрешение'))}
}
function stopQRScanner(){if(apgScanTimer)cancelAnimationFrame(apgScanTimer);apgScanTimer=null;if(apgScannerStream){apgScannerStream.getTracks().forEach(t=>t.stop());apgScannerStream=null}const v=document.getElementById('qrVideo');if(v)v.srcObject=null}
const oldPage=page;
page=function(html,title='APG'){oldPage(html,title);setTimeout(async()=>{try{let a=await api('/api/notifications');let unread=a.filter(x=>!x.read).length;document.querySelector('.top .btn')?.insertAdjacentHTML('beforebegin',`<button class="notify-btn" onclick="notifications()">◉${unread?`<i>${unread>9?'9+':unread}</i>`:''}</button>`)}catch(e){}},0)};
const oldCompanyPage=companyPage;
companyPage=async function(id,tab='offers'){let c=await api('/api/companies/'+id);let tabs=['offers','gallery','services','reviews'];page(`<div class="detail-head"><div class="biglogo">${c.cover?`<img src="${c.cover}">`:c.logo?`<img src="${c.logo}">`:esc(c.name[0])}</div><div class="card"><span class="badge">${esc(c.category)}</span><h1>${esc(c.name)}</h1><p class="muted">${esc(c.description)}</p><div class="list"><div class="listitem">⌖ ${esc(c.address)}</div><div class="listitem">☎ ${esc(c.phone)}</div><div class="listitem">◷ ${esc(c.hours)}</div></div><div class="row" style="margin-top:12px">${c.address?`<a class="btn" href="${mapsLink(c.address)}" target="_blank" rel="noopener">Маршрут</a>`:''}<button class="btn" onclick="shareText('${esc(c.name)}')">Поделиться</button><button class="btn" onclick="chatPage(${id})">Чат</button>${me.role==='customer'?`<button class="btn primary" onclick="bookModal(${id})">Записаться</button>`:''}${me.role==='customer'?`<button class="btn primary" onclick="fav(${id})">${'Добавить в избранное'}</button>`:''}</div></div></div><div class="tabs" style="margin-top:20px">${tabs.map(t=>`<button class="btn ${tab===t?'primary':''}" onclick="companyPage(${id},'${t}')">${({offers:'Скидки',gallery:'Галерея',services:'Услуги',reviews:'Отзывы'})[t]}</button>`).join('')}</div>${tab==='offers'?companyOffers(c):tab==='gallery'?companyGallery(c):tab==='services'?companyServices(c):companyReviews(c)}`,'Компания')};
const oldProfile=profile;
profile=async function(){let d=await api('/api/profile');let u=d.user,s=d.settings;page(`<div class="grid2"><div class="card"><div class="avatar" style="width:96px;height:96px">${u.avatar?`<img src="${u.avatar}">`:esc(u.name[0])}</div><h2>${esc(u.name)}</h2><div class="muted">${esc(u.email)}</div><span class="badge">${esc(u.role)}</span><div class="divider"></div><form onsubmit="event.preventDefault();saveProfile(this)"><div class="field"><label>ИМЯ</label><input name="name" value="${esc(u.name)}"></div><div class="field"><label>ТЕЛЕФОН</label><input name="phone" value="${esc(u.phone)}"></div><div class="field"><label>USERNAME</label><input name="username" value="${esc(u.username)}"></div><div class="field"><label>АВАТАР</label><input name="avatar" type="file" accept="image/*"></div><button class="btn primary">Сохранить профиль</button></form></div><div class="card"><h2>APG на устройстве</h2><p class="muted">Установи APG как приложение на телефон или компьютер.</p><button class="btn primary" onclick="installAPG()">Установить APG</button><div class="divider"></div><h2>Визуальный кастом</h2><form onsubmit="event.preventDefault();saveSettings(this)"><div class="field"><label>АКЦЕНТ</label><select name="accent"><option value="white">White</option><option value="silver">Silver</option><option value="lime">Lime</option><option value="blue">Blue</option></select></div><div class="field"><label>ПЛОТНОСТЬ</label><select name="density"><option value="comfortable">Comfortable</option><option value="compact">Compact</option></select></div><label><input type="checkbox" name="animations" checked> Анимации</label><br><br><button class="btn primary">Применить</button></form></div></div>`,'Профиль')}

if ('serviceWorker' in navigator) window.addEventListener('load',()=>navigator.serviceWorker.register('/static/sw.js').catch(()=>{}));


async function bookingsPage(){
  let rows=await api('/api/bookings');
  page(`<div class="split"><div><h2>Записи на услуги</h2><p class="muted">Онлайн-запись клиентов и управление статусами.</p></div></div><div class="grid">${rows.map(b=>`<div class="card"><div class="split"><b>${esc(b.company_name)}</b><span class="badge ${b.status==='confirmed'?'good':b.status==='cancelled'?'bad':'warn'}">${esc(b.status)}</span></div><h3>${esc(b.service_name||'Услуга')}</h3><p>${esc(b.slot)}</p><p class="muted">${esc(b.user_name||'Ты')} ${b.car_model?'· '+esc(b.car_model):''}</p><p>${esc(b.note||'')}</p>${me.role!=='customer'?`<div class="row"><button class="btn primary" onclick="bookingStatus(${b.id},'confirmed')">Подтвердить</button><button class="btn" onclick="bookingStatus(${b.id},'completed')">Завершить</button><button class="btn danger" onclick="bookingStatus(${b.id},'cancelled')">Отменить</button></div>`:''}</div>`).join('')||'<div class="empty">Записей пока нет</div>'}</div>`,'Записи');
}
async function bookingStatus(id,status){let f=new FormData();f.append('status',status);f.append('csrf',window.apgCsrf||'');await api('/api/bookings/'+id,{method:'PATCH',body:f});bookingsPage()}
async function bookModal(cid){let c=await api('/api/companies/'+cid);let opts=c.services.map(x=>`<option value="${x.id}">${esc(x.title)} — ${esc(x.price||'по запросу')}</option>`).join('');modal('Запись на услугу',`<form onsubmit="createBooking(event,${cid})"><label>Услуга<select name="service_id">${opts||'<option value="0">Выбери услугу</option>'}</select></label><label>Дата и время<input name="slot" type="datetime-local" required></label><label>Автомобиль ID<input name="car_id" type="number" min="0" placeholder="необязательно"></label><label>Комментарий<textarea name="note" maxlength="1000"></textarea></label><button class="btn primary" type="submit">Записаться</button></form>`)}
async function createBooking(e,cid){e.preventDefault();let f=new FormData(e.target);f.append('company_id',cid);f.append('csrf',window.apgCsrf||'');await api('/api/bookings',{method:'POST',body:f});document.querySelector('.modal')?.remove();toast('Запись создана');go('bookings')}
async function chatPage(cid){let d=await api('/api/chat/'+cid);modal('Чат с компанией',`<div id="chatbox" class="chatbox">${(d.messages||[]).map(m=>`<div class="chatmsg ${m.sender_id===me.id?'mine':''}"><b>${esc(m.sender_name)}</b><div>${esc(m.body)}</div><small>${esc(m.created_at)}</small></div>`).join('')||'<div class="empty">Начни диалог</div>'}</div><form onsubmit="sendChat(event,${cid})" class="row"><input name="body" maxlength="2000" placeholder="Сообщение…" required style="flex:1"><button class="btn primary">Отправить</button></form>`)}
async function sendChat(e,cid){e.preventDefault();let f=new FormData(e.target);f.append('csrf',window.apgCsrf||'');await api('/api/chat/'+cid,{method:'POST',body:f});chatPage(cid)}
async function promosPage(){let ps=await api('/api/promo');page(`<div class="split"><div><h2>Промокоды</h2><p class="muted">${me.role==='owner'?'Создание и контроль промокодов APG.':'Активные промокоды для клиентов.'}</p></div>${me.role==='owner'?'<button class="btn primary" onclick="promoModal()">Создать</button>':''}</div><div class="grid">${ps.map(p=>`<div class="card"><span class="badge good">${esc(p.code)}</span><h3>${esc(p.discount||'Спецусловие')}</h3><p>${esc(p.description)}</p><div class="muted">Использовано: ${p.used_count}${p.max_uses?' / '+p.max_uses:''}</div>${me.role==='customer'?`<button class="btn primary" style="margin-top:12px" onclick="redeemPromo('${esc(p.code)}')">Активировать</button>`:''}</div>`).join('')||'<div class="empty">Промокодов пока нет</div>'}</div>`,'Промокоды')}
function promoModal(){modal('Новый промокод',`<form onsubmit="createPromo(event)"><label>Код<input name="code" required maxlength="40" placeholder="APG2026"></label><label>Описание<textarea name="description"></textarea></label><label>Скидка<input name="discount" placeholder="10%"></label><label>До<input name="expires_at" type="datetime-local"></label><label>Лимит использований<input name="max_uses" type="number" min="0" value="0"></label><button class="btn primary">Создать</button></form>`)}
async function createPromo(e){e.preventDefault();let f=new FormData(e.target);f.append('csrf',window.apgCsrf||'');await api('/api/owner/promo',{method:'POST',body:f});document.querySelector('.modal')?.remove();promosPage()}
async function redeemPromo(code){let f=new FormData();f.append('code',code);f.append('csrf',window.apgCsrf||'');await api('/api/promo/redeem',{method:'POST',body:f});toast('Промокод активирован');promosPage()}
async function plansPage(){let ps=await api('/api/plans');page(`<div class="split"><div><h2>Тарифы партнёров</h2><p class="muted">Коммерческий слой APG для подключённых компаний.</p></div>${me.role==='owner'?'<button class="btn primary" onclick="planModal()">Добавить тариф</button>':''}</div><div class="grid3">${ps.map(p=>`<div class="card plan"><span class="badge">${esc(p.period)}</span><h2>${esc(p.name)}</h2><p>${esc(p.description)}</p><div class="price">${esc(p.price)}</div>${me.role==='seller'?'<button class="btn primary" onclick="toast(\'Выбери тариф и согласуй подключение с Owner\')">Подключить</button>':''}</div>`).join('')}</div>`,'Тарифы')}
function planModal(){modal('Новый тариф',`<form onsubmit="createPlan(event)"><label>Название<input name="name" required></label><label>Описание<textarea name="description"></textarea></label><label>Цена<input name="price" placeholder="29 €"></label><label>Период<input name="period" value="month"></label><button class="btn primary">Создать</button></form>`)}
async function createPlan(e){e.preventDefault();let f=new FormData(e.target);f.append('csrf',window.apgCsrf||'');await api('/api/owner/plans',{method:'POST',body:f});document.querySelector('.modal')?.remove();plansPage()}
