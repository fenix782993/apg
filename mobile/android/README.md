# Android shell

This folder is the Android target metadata for the APG Capacitor application.

From `mobile/` after installing Node.js:

```cmd
npm install
npx cap add android
npx cap sync android
npx cap open android
```

Build/sign the release APK/AAB in Android Studio. Configure camera, location and notification permissions in the generated native project.
